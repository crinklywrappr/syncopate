;; Purely additive schema migration (:schema/create). No :down needed — Syncopate
;; derives it automatically (creating :user/id + :user/name inverts to removing).
{:up {:schema/create {:user/id   {:db/valueType :db.type/long
                                  :db/unique    :db.unique/identity}
                      :user/name {:db/valueType :db.type/string}}}}
