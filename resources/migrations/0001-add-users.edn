;; Purely additive schema migration. No :down needed — Syncopate derives it
;; automatically (adding :user/id + :user/name inverts to removing them).
{:up {:schema {:user/id   {:db/valueType :db.type/long
                           :db/unique    :db.unique/identity}
               :user/name {:db/valueType :db.type/string}}}}
